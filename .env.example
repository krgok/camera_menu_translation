# Client-side (Vite exposes VITE_* vars to the browser; safe to expose — protected by RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (used by api/analyze.ts, never exposed to the browser)
SUPABASE_URL=
SUPABASE_ANON_KEY=
GOOGLE_VISION_API_KEY=
GEMINI_API_KEY=
